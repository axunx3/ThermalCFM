# Default configuration for Thermal CFM Framework
seed: 42
device: cuda

# Forward model selection (the only thing you change between methods)
forward_model:
  name: flash  # flash | 3omega | tdtr

# Experiment tracking
wandb:
  project: thermal-cfm
  entity: null
  mode: online
